<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="web.FontAwesomeIconField">
        <t t-if="props.readonly" t-out="iconValue" />
        <t t-else="">
            <SelectMenu
                autoSort="false"
                class="'o_fa_icon_field'"
                menuClass="'o_fa_icon_field_selector_menu'"
                togglerClass="'o_fa_icon_toggler'"
                value="iconValue"
                choices="choices"
                onSelect="onSelect"
                placeholder.translate="Select icon"
                searchPlaceholder.translate="Search icon">

                <!-- Toggler (what user clicks) -->
                <t t-slot="default">
                    <div class="o_fa_icon_toggler_inner">
                        <t t-out="iconValue"/>
                    </div>
                </t>

                <t t-set-slot="choice" t-slot-scope="choice">
                    <i t-att-class="'o_font_awesome_icon_selector_value  d-inline-flex justify-content-center align-items-center fs-3 ' + choice.data.value" t-att-data-tooltip="getIconTooltip(choice.data.value)" />
                </t>
            </SelectMenu>
        </t>
    </t>
</templates>

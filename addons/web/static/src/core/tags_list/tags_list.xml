<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="web.TagsList">
        <t t-foreach="this.visibleTags" t-as="tag" t-key="tag.id or tag_index">
            <t t-slot="default" t-props="tag"/>
        </t>
        <t t-if="this.invisibleTags.length">
            <!-- COLOR 99 is a hack to force the bg color with a css class -->
            <BadgeTag color="99" cssClass="'bg-secondary o_m2m_avatar_empty'" text="'+' + this.invisibleTags.length" tooltip.translate="Click to show more" t-on-click.stop="showAllTags"/>
        </t>
    </t>

</templates>
